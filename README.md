# Finance Telegram Mini App

MVP-проект для учета личных финансов через Telegram Bot + Telegram Mini App.

## Что уже сделано

- создана новая отдельная директория
- добавлен базовый проект на Node.js + TypeScript
- подготовлена минимальная структура для дальнейших шагов

## Самый первый шаг

Мы начали с самого простого:

1. создали новый пустой проект
2. подключили TypeScript
3. добавили стартовую точку входа `src/index.ts`

## Что будет следующим шагом

Следом лучше сделать основу данных:

- сущности проекта
- schema для Supabase
- SQL-таблицы для MVP

Это правильный порядок, потому что бот, мини-апп и отчеты будут опираться именно на структуру базы данных.

## Что уже сделано на втором шаге

Теперь в проекте уже есть:

- `supabase/migrations/001_init_mvp.sql`
- `src/shared/domain.ts`
- `docs/step-02-database.md`

То есть база для MVP уже спроектирована на уровне структуры данных.

## Что уже сделано на третьем шаге

Теперь в проекте уже есть базовая интеграция:

- Telegram Bot
- Supabase client
- `.env.example`
- команда `/start`

Команда `/start` пока делает самое важное для начала:

- получает Telegram-пользователя
- создает или обновляет запись в `app_users`

## Что уже сделано на четвертом шаге

Теперь бот умеет:

- создавать счета
- показывать список счетов
- использовать простые кнопки для первого сценария

## Что уже сделано на пятом шаге

Теперь проект умеет работать как настоящий Telegram Mini App:

- бот показывает кнопку открытия приложения
- Node.js сервер отдает mini app и API
- mini app показывает счета
- mini app умеет создавать счета через форму

## Что уже сделано на шестом шаге

Теперь mini app умеет:

- показывать дашборд
- создавать категории
- добавлять доходы и расходы
- обновлять баланс счетов
- показывать последние операции

## Что уже сделано на седьмом шаге

Теперь mini app умеет:

- переводить деньги между счетами
- обновлять курсы валют через API
- считать общий баланс с конвертацией
- строить отчеты по периоду

## Что уже сделано на восьмом шаге

Теперь mini app умеет:

- работать с расширенным списком валют
- искать валюту при создании счета
- быстро выбирать популярные валюты
- выбирать валюту отчета прямо в интерфейсе

## Деплой на Railway

1. Создайте проект на [Railway](https://railway.app), подключите этот репозиторий, добавьте **один сервис** из корня репозитория.
2. Во вкладке **Variables** задайте как минимум: `TELEGRAM_BOT_TOKEN`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `APP_URL`.
   - **APP_URL**: `https://${{RAILWAY_PUBLIC_DOMAIN}}` — подставится публичный URL сервиса (без финального `/`). Допустимо указать только домен без `https://` — сервер сам допишет схему (Telegram иначе отклонит кнопку Web App).
   - Рекомендуется **TELEGRAM_WEBHOOK_SECRET** (случайная строка): тот же секрет укажите в Telegram при настройке webhook, если используете проверку заголовка.
3. **PORT** задаёт Railway сама. При наличии переменной **`PORT`** сервер по умолчанию слушает **`0.0.0.0`**, чтобы healthcheck по IPv4 доходил (см. `src/config/env.ts`).
4. Если деплой падает на **Healthcheck**: откройте **Deploy Logs**. Частые причины: не заданы секреты; в Variables указан **`HOST=::`** — удалите `HOST` или задайте **`HOST=0.0.0.0`** (healthcheck Railway ходит по IPv4). В `railway.json` старт уже с **`HOST=0.0.0.0`**, но явная переменная в UI переопределяет её.
5. После первого успешного деплоя откройте домен сервиса, убедитесь что `GET /health` отвечает `{"ok":true}`.
6. В Telegram бот должен принимать webhook на `https://<ваш-домен>/api/telegram/webhook` — это настраивается автоматически при старте, если задан `APP_URL`.

В репозитории лежит `railway.json`: сборка `npm ci --include=dev && npm run build` (так `typescript` из devDependencies точно есть при `NODE_ENV=production` на этапе установки), старт `HOST=0.0.0.0 node dist/index.js`, healthcheck `/health`. Версия Node: **`engines.node`** и **`.nvmrc`** (22) — на Node 22+ у runtime есть встроенный WebSocket для Supabase; для Node 20 в коде остаётся полифилл `ws`.

## Teams MVP (рабочие пространства)

После деплоя backend и mini app с поддержкой команд:

1. В Supabase SQL Editor по порядку примените миграции из `supabase/migrations/` (минимум `001` … `005_workspaces_mvp.sql`, рекомендуется `006_workspace_transfer_account_indexes.sql`).
2. Проверьте базу: `npm run verify:workspaces` (полный проход: `node scripts/verify-phase-1-workspaces.mjs --full`).
3. Интеграционные тесты сервисов: `npm run test:workspaces` (создаёт и удаляет тестовых пользователей в Supabase).
4. Перезапустите сервис на Railway.

### Поведение

- У каждого пользователя есть **личное** пространство; опционально одна **команда** (до 5 участников).
- Активное пространство хранится в cookie `balancy_workspace_id` (веб и mini app).
- Веб: `https://<домен>/mini-app/?web=1`, приглашение: `?web=1&invite=<token>`.
- В Telegram mini app: переключение в «Ещё → Команда и доступ», настройки команды в «Настройки».
- В командном режиме в истории операций видно, **кто внёс** запись.
- **Telegram-бот** (`/accounts`, `/add`) работает только с **личными** счетами; команда — через mini app / веб.

### API (основное)

- `POST /api/workspaces/team` — создать команду
- `POST /api/workspaces/switch` — переключить пространство
- `POST /api/workspaces/invites` / `GET /api/workspaces/invites` — ссылка и список активных приглашений (владелец)
- `POST /api/workspaces/invites/:token/accept` — принять приглашение
- `POST /api/workspaces/team/leave` — покинуть команду (не владелец)
